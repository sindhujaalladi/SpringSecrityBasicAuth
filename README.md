# SpringSecrityBasicAuth

Here we are implementing spring security which is basic auth
we have implemented following validations 
a) whatever the request hitting the service it should be fully authenthicated 
b) whatever the request URI hitting the service which starts from REST it should be fully authenthicated
c) whatever the request URI hitting the service which starts from REST  which has ADMIN role it should be fully authenthicated
